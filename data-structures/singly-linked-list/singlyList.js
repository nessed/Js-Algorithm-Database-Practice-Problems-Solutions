class Node {
  constructor(val) {
    this.val = val;
    this.next = null;
  }
}

class SinglyLinkdList {
  constructor() {
    this.head = null;
    this.tail = null;
    this.length = 0;
  }
  push(val){

  }
}
var list = new SinglyLinkdList()
list.push("Hello")
list.push("Goodbye")

// let first = new Node("Hi");
// first.next = new Node("there");
// first.next.next = new Node("ali");
// first.next.next.next = new Node("are");
// first.next.next.next.next = new Node("you");

// console.log(first);
